"""AI Engineering Assistant (F-005): an engineer, not a chat.

`knowledge` holds the deterministic tables and rules (materials, walls, fasteners, fits);
`assistant` turns a question about a model into a verdict with numbers from the facts and,
where the fix is expressible, an OperationPlan validated like any other.
"""
