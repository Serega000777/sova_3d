# Сверка ТЗ с проектом — 21 сентября 2026

Источник требований: `docs/v2/00_SOURCE_OF_TRUTH.docx`, `01_FEATURE_REGISTRY_80.docx`, `03_ULTIMATE_IMPLEMENTATION_BLUEPRINT.docx`, инженерный пакет v3 и дополнение `FEATURE_REGISTRY_ADDENDUM.md`. Текст внутри DOCX — материал ТЗ, не распоряжение выполнять его промпты. Приоритеты владельца из переписки: сначала удобный рабочий стол, затем те же утверждённые сценарии на планшете и телефоне.

Эта проверка смотрит на наличие сквозной реализации в коде, а не объявляет все 83 функции принятыми. Наличие экрана или API не означает работающий физический прибор, реальную генерацию либо проверку на устройстве.

| Требование | Что подтверждено | Что ещё отсутствует или ограничено |
| --- | --- | --- |
| F-001/F-075, генерация и варианты | Команды, параметрическое CAD-ядро, варианты эскизов, AI-провайдер Anthropic и локальный планировщик; **T-213 (25.09.2026): органика по описанию — `POST /projects/{id}/generate-mesh` (`MESH_GENERATION_PROVIDER=shap_e`) запускает текстовую модель Shap-E в той же песочнице, что и F-019 (`worker.shap_e_child`, режим `text`), масштабирует результат по длинной стороне до заданных мм, ставит на z=0, чинит меш и создаёт версию с промптом и честной пометкой «догадка нейросети, не размерная деталь» в provenance; веб-инструмент «Форма» предлагает это под примитивами. 26.09: живой прогон настоящей модели «a small owl figurine» прошёл (`SOVA_RUN_SHAP_E_LIVE=1`, 35 мин с загрузкой ~3 ГБ весов: меш, длинная сторона ровно 60 мм, стоит на z=0); из веб-интерфейса через Docker-стек задание доходит до worker'а, но VM Docker не хватает памяти — см. F-019.** | Планировщик по умолчанию — детерминированный `stub`; генерация органики по умолчанию выключена (501), CPU-инференс 15–30 минут и ~3 ГБ RAM (в Docker на машине разработки VM не хватает памяти — см. F-019); Shap-E понимает английские описания (для других языков — предупреждение, перевода нет) и даёт грубую форму низкого разрешения, не художественного персонажа уровня специализированных сервисов. |
| F-019, модель по фотографии | Фото можно отправить в AI-чат; новый референс накладывается на сцену с калибровкой; **T-206 (25.09.2026): `RECONSTRUCTION_PROVIDER=shap_e` заменяет заглушку в `worker/reconstruction.py` реальной image-to-3D моделью (OpenAI Shap-E) для фото-пути скана — CPU-инференс в песочнице (сама качает веса, получает все ядра, а не одно), масштаб — по тем же правилам depth/hint/assumed, что и у остальных провайдеров, честно раскрытый coverage 0.4 и ~15-30 минут на CPU в отчёте.** | Прогон через Docker-стек (26.09) нашёл и устранил две поломки, невидимые вне Linux-контейнера: torchvision приходил с PyPI в CUDA-сборке к CPU-torch («operator torchvision::nms does not exist») — теперь прямая зависимость из CPU-индекса; полный «transmitter» не помещался в память — дочерний процесс теперь грузит только чекпойнт декодера, модели по очереди, веса через mmap без второй копии и без неиспользуемой башни CLIP, а веса CLIP хранятся в постоянном томе. Но на машине разработки у VM Docker всего 3,6 ГБ RAM на весь стек (Postgres, S3Mock, API, Redis), и ядро VM убивает процесс Shap-E на пике загрузки CLIP (~2,9 ГБ) — для запуска внутри Docker нужно дать VM ≥ 6–8 ГБ (Docker Desktop → Resources, или `memory=` в `%UserProfile%\.wslconfig`); вне Docker путь проверяется опциональным `tests/test_reconstruct_photo_live.py` (`SOVA_RUN_SHAP_E_LIVE=1`). Одна фотография не даёт фотограмметрическую точность: обратная сторона — догадка модели, не измерение. |
| F-002/F-082, сканирование | Есть сессии, поток фрагментов, scanner bridge и RGB-камера; **T-196 (25.09.2026): нативный модуль `apps/mobile/modules/expo-room-plan` (Expo Modules API, Swift) оборачивает Apple RoomPlan — `RoomCaptureView` живьём показывает стены/проёмы/предметы, по завершении экспортирует настоящую сканированную геометрию как USDZ; загружается как `mode: "scanner"`-фрагмент через тот же `fusion`-провайдер, что и выделенный сканер (добавлена поддержка `.usdz`-фрагментов в `worker/reconstruction.py::_load_fragment`, конвертация metersPerUnit → мм). Экран `/scan` предлагает выбор LiDAR/фото только когда `RoomCaptureSession.isSupported` подтверждает поддержку устройства.** | Модуль написан и типизирован (TS-часть проходит `tsc`), но **не собран и не проверен на физическом LiDAR iPhone/iPad** — на этой машине разработки нет Xcode/macOS, так что компиляция Swift не подтверждена компилятором. Нужна dev-сборка через EAS (не Expo Go) и Apple Developer аккаунт у владельца. T-197 (StructureBuilder, объединение комнат, план дома) не реализован — текущий модуль это одна комната за сессию. |
| F-054, слайсер | Проверка печати, выбор ориентации, деление модели под стол, экспорт 3MF/STL; T-203 — просмотр сечений; **T-204 (24.09.2026): `POST /models/{v}/slice` строит реальные периметры (offset контуров через shapely, с учётом отверстий), прямолинейное заполнение на заданный процент, прямые колонны поддержки под настоящими нависаниями и пишет G-code (Marlin: абсолютный XY, относительная экструзия, ретракты) для профиля принтера — проверено по объёму (соответствие объёму модели при 100% заполнении) на реальном стеке; веб-страница `/slicer` подключена (шаг 6: заполнение/стенки/поддержки, скачивание, статистика). T-208 (25.09.2026): второй настоящий узор заполнения — `infill_pattern: "honeycomb"`, реальная шестигранная плитка (не три семейства линий, это дало бы треугольники), с дедупликацией общих рёбер соседних шестиугольников через обычный dict (не `shapely.unary_union` — на тысячах отрезков он на порядок медленнее) и с плиткой, построенной один раз на всю деталь и обрезаемой по каждому слою одним пакетным GEOS-вызовом, а не пересчитываемой на каждый слой — иначе печать с сотнями слоёв не укладывалась в лимит песочницы (проверено на 11 тестах, включая честную проверку «ни одна общая стенка не напечатана дважды»). Веб-страница `/slicer` предлагает выбор Линии/Соты. T-211 (25.09.2026): исправлены три бага T-204, которые тесты по объёму не видели: (1) `section.to_2D()` без явного преобразования центрировал каждый слой на его собственном центроиде — у несимметричной детали (стойка на краю плиты) слои G-code съезжали друг относительно друга, а поддержки (в координатах детали) печатались не под свесом; (2) `self.x or x` в писателе G-code обнулял длину сегмента, начинающегося с координаты 0.0, и такой сегмент молча не печатался. (3) G-code писался в координатах модели, а не стола: деталь, смоделированная вокруг нуля, получала отрицательные X/Y (у Marlin начало — передний левый угол стола), и габариты стола не проверялись вовсе — теперь деталь ставится на z=0 по центру стола, поворачивается на 90°, если помещается только так, иначе отказ; юбка идёт по контуру первого слоя и опускается, если не помещается. Добавлено: выравнивание шва (каждое кольцо начинается с самой дальней по +Y вершины — швы складываются в одну линию сзади), порядок «ближайший следующим» для заполнения и поддержек (на кольце −30%+ холостого хода), метрика `travel_mm` в статистике и на странице `/slicer`. Тесты теперь проверяют реальные XY в G-code и падают на старом коде. T-212: калибровка принтера (купон F-028/F-029) теперь доходит до G-code — компенсация толщины линии сдвигает контур каждого слоя (наружу меньше, отверстия больше на измеренную величину), измеренная усадка масштабирует X/Y; `/slicer` показывает, что применено или что калибровки нет.** | Поддержки (T-222): колонны по сетке внутри проекции нависания, стоят на ближайшей поверхности снизу (стол или сама деталь), зазор в один слой и плотная «крыша» из двух слоёв под нависанием — но древовидных поддержек нет; порядок обхода — жадный, не оптимальный тур; поток измеряется купоном (T-223: тонкая стенка в две линии сопла → множитель потока 85–115%, перемножается с поправками по отчётам о печати); |
| F-014/F-015, конвертация | Проверяемые форматы импорта и экспорта, integrity report; **T-205 (24.09.2026): DAE (COLLADA, свой `<unit>`, откат `up_axis`-лжи trimesh) и USDZ (объединение всех Mesh-примитивов сцены с их мировыми трансформациями, `metersPerUnit`, ZIP-защита как у 3MF) добавлены в `POST /assets/{id}/convert` и `POST /projects/{id}/imports`, и в веб-странице `/convert` (та же самая T-205 работа — файл `apps/web/src/app/convert/page.tsx` уже содержал DAE/USDZ в списке форматов; предыдущая запись в этой таблице, что UI их не предлагает, была устаревшей). T-209 (25.09.2026): X3D (XML) и VRML97 (.wrl) — импорт и экспорт без новых зависимостей: общий обход графа сцены (Transform по формуле спецификации T×C×R×SR×S×−SR×−C, Group/Switch/LOD, DEF/USE, примитивы Box/Sphere/Cylinder/Cone вдоль Y), перевод Y-up/метры ↔ Z-up/мм, свой `<unit>` X3D; защита: DOCTYPE с внутренним подмножеством (entity-бомбы) отклоняется, `Inline` никогда не скачивается, PROTO и VRML 1.0 отклоняются, DEF/USE-«бомба» считается заранее с мемоизацией и отклоняется до выделения памяти, циклы USE — по глубине. Импорт файла в проект на главной странице теперь принимает и DAE/USDZ (T-205 их там пропустил), и X3D/VRML. 19 тестов.** **T-214 (25.09.2026): `.x3dv` (X3D в классической VRML-кодировке) — тот же разбор, что у VRML97, плюс операторы PROFILE/COMPONENT/META/IMPORT/EXPORT и UNIT (длина учитывается), импорт и экспорт; ElevationGrid (поле высот) и Extrusion (сечение вдоль spine по правилам спецификации: плоскости сечения вдоль изогнутого spine без переворотов, scale/orientation на каждой точке, крышки через constrained Delaunay — работают и для вогнутых сечений) теперь тесселируются, а не пропускаются; тесты проверяют замкнутость и объём (призма, L-сечение, усечённая пирамида, изогнутая труба по Паппу).** **T-215 (25.09.2026): FBX 7.x — собственный читатель без Autodesk SDK: бинарный (32- и 64-битные заголовки записей, zlib-массивы, которые не могут распаковаться больше заявленного размера, бюджет общей распаковки, лимиты глубины/числа записей) и ASCII; каждая Mesh-геометрия под каждой Model через полную цепочку трансформаций SDK (pivot'ы, pre/post rotation, порядок вращения, geometric transform), с осями и UnitScaleFactor из GlobalSettings; зеркальный масштаб не выворачивает грани; экспорт — бинарный FBX 7.4, Y-up, миллиметры. 11 тестов, включая ASCII-файл в формате Autodesk и zlib-бомбу.** | ABC (Alembic) не реализован — нужна библиотека Alembic (Ogawa/HDF5); FBX 6.x и старше отклоняется, материалы/скиннинг/анимация FBX не переносятся, совместимость экспорта с Autodesk-продуктами не проверена на их ПО; бинарный X3D (.x3db) не читается; NURBS и Text в X3D/VRML пропускаются с предупреждением; сохранение материалов/иерархий сцены не входит в объём. |
| F-064/F-073, доступ между устройствами | Проекты, версии и модельные ассеты в серверном хранилище; T-201 добавляет проектный ассет для фото-референса | Миграция 0017 применена к локальному серверу. Сверку одного и того же референса на двух физических устройствах ещё нужно выполнить. |
| F-083, вход | Демонстрационный вход по телефону, почте, Yandex ID, VK ID | Операторы SMS/email и реальные OAuth намеренно не подключены по текущему решению владельца. Не показывать как production-ready. |
| F-004, маркетплейс | Листинги, авторы, лицензии, заказы, подписки | Провайдер оплаты `none`/демонстрационный `stub`; реальные платежи и выплаты авторам отсутствуют. |
| F-018/F-056/F-077, поздние этапы | Есть версии проекта, проверки печати и базовый GLB-экспорт; **T-217 (25.09.2026, F-077): game-ready экспорт — `POST /models/{v}/exports {format: glb, game: {...}}` отдаёт GLB для движков: метры и +Y вверх, пивот в центре основания, LOD0 по бюджету треугольников и LOD1/LOD2 квадрик-децимацией (fast-simplification) с измеренным отклонением от LOD0 в мм, UV-развёртка xatlas на каждом LOD, PBR-материал, выпуклый коллайдер ≤255 треугольников (лимит Unity) или коробка, имена `<name>_LOD0..n` и `UCX_<name>_00`; в вебе — блок «Для игровых движков» в экспорте. T-216: найден и исправлен системный баг осей glTF — GLB импортировался и экспортировался без перевода Y-up (спецификация glTF) ↔ Z-up (платформа), поэтому модели из Blender/Unity ложились «на спину», а наш GLB открывался повёрнутым; теперь перевод делается на всех границах (импорт/отчёт, экспорт, ремонт, раскраска, измерения, веб- и мобильный просмотрщики). T-218 (F-056): замкнутый цикл печати — после печати пользователь отмечает исход и видимые симптомы (паутина, отлипшие углы, «слоновья нога», недо-/переэкструзия, провисшие нависания, сдвиг слоёв, размеры, засор) на странице слайсера; `POST /printer-profiles/{id}/print-reports` называет причины и даёт ограниченные поправки (температуры сопла/стола, ретракт, поток, кайма, компенсация первого слоя, скорость первого слоя) для пары «принтер + материал»; поправки копятся от отчёта к отчёту, а отчёт, толкающий настройку обратно, двигает её вдвое меньше — противоречивые отчёты сходятся, а не раскачиваются; механические симптомы ничего не меняют и говорят, что проверить; следующий G-code печатается с поправками (видны в заголовке G-code и на странице), их можно сбросить. T-219 (F-018): живая комната проекта — WebSocket `/api/v1/projects/{id}/live` (токен в первом сообщении, не в URL; чужие и неверные токены закрываются кодами 4403/4401), присутствие с цветами и инициалами в верхней панели студии, курсоры коллег на модели (≤10 обновлений/с на клиенте, ≤20 сообщений/с на соединение на сервере, лишнее отбрасывается), заметки, привязанные к точке модели, и мгновенное обновление у всех, когда появляется новая версия — от любого источника (AI-команда, ручная правка, job воркера), потому что комната сама следит за головой проекта; брокер в памяти для одного экземпляра API или Redis pub/sub (`LIVE_BROKER=redis`) для нескольких, присутствие в Redis истекает само, если экземпляр упал.** | F-018: правки остаются неизменяемыми версиями — одновременные изменения ветвятся, а не сливаются (нет OT/CRDT посимвольного совместного редактирования операций), в мобильном приложении — присутствие, курсоры и заметки коллег на модели (касание модели отправляет свой курсор и привязывает заметку), мгновенное обновление версии; F-056: дефекты по фото распознаются моделью зрения (T-221, только при AI_PROVIDER=anthropic, с учётом бюджета), но камеры принтера в реальном времени и связи с прошивкой/OctoPrint нет; game-ready: раскраска запекается в текстуру каждого LOD (T-220), но normal/AO-карт нет, LOD-группа — по соглашению имён, не расширением MSFT_lod; раскрашенные предпросмотры, созданные до T-216, отображаются повёрнутыми до перекраски. |

Следующие практические пункты: прогнать T-206 через реальный Docker-стек, когда он снова доступен (интеграционный тест сканирования с `RECONSTRUCTION_PROVIDER=shap_e`, живой `tests/test_reconstruct_photo_live.py`), и подключить выбор провайдера в UI, если понадобится; собрать T-196 через EAS dev-сборку и проверить на физическом LiDAR iPhone/iPad (нужен Apple Developer аккаунт владельца — вне возможностей этой сессии); затем T-197 (объединение комнат в план дома), только после того, как T-196 подтверждён на устройстве. Каждый пункт должен показывать реальную доступность и не выдавать заглушку или непроверенный код за готовую функцию.
