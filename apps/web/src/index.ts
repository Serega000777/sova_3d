// Placeholder workspace package for the web client. The real application
// shell is scheduled as its own task (see docs/codex_tasks.json, epic E10).
import { OPERATION_PLAN_SCHEMA_VERSION } from "@physical-ai/contracts";

export const APP_NAME = "web";
export const SUPPORTED_PLAN_SCHEMA_VERSION = OPERATION_PLAN_SCHEMA_VERSION;
